[project]
name = "gomoku-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
conflicts = [
  [
    { package = "gomoku-training", extra = "cpu" },
    { package = "gomoku-training", extra = "xpu" },
    { package = "gomoku-training", extra = "cuda" },
  ],
]

[tool.uv.workspace]
members = ["apps/desktop", "tools", "training"]
