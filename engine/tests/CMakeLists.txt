add_executable(gomoku-engine-tests test_engine.cpp)
target_link_libraries(gomoku-engine-tests PRIVATE gomoku_engine)
if(EMSCRIPTEN)
  # Test executables use Emscripten's CommonJS launcher inside this ESM monorepo.
  set_target_properties(gomoku-engine-tests PROPERTIES SUFFIX .cjs)
endif()
add_test(NAME engine COMMAND gomoku-engine-tests)
add_executable(gomoku-nnue-kernel-tests test_nnue_kernels.cpp)
target_link_libraries(gomoku-nnue-kernel-tests PRIVATE gomoku_engine)
if(EMSCRIPTEN)
  set_target_properties(gomoku-nnue-kernel-tests PROPERTIES SUFFIX .cjs)
endif()
add_test(NAME nnue-kernels COMMAND gomoku-nnue-kernel-tests)
add_executable(gomoku-search-tests test_search.cpp)
target_link_libraries(gomoku-search-tests PRIVATE gomoku_engine)
if(EMSCRIPTEN)
  set_target_properties(gomoku-search-tests PROPERTIES SUFFIX .cjs)
endif()
add_test(NAME search-reference COMMAND gomoku-search-tests)
add_executable(gomoku-vcf-tests test_vcf.cpp)
target_link_libraries(gomoku-vcf-tests PRIVATE gomoku_engine)
if(EMSCRIPTEN)
  set_target_properties(gomoku-vcf-tests PROPERTIES SUFFIX .cjs)
endif()
add_test(NAME vcf COMMAND gomoku-vcf-tests)
if(TARGET nlohmann_json::nlohmann_json)
  add_executable(gomoku-nnue-check nnue_check.cpp)
  target_link_libraries(gomoku-nnue-check PRIVATE gomoku_engine nlohmann_json::nlohmann_json)
  if(EMSCRIPTEN)
    set_target_properties(gomoku-nnue-check PROPERTIES SUFFIX .cjs)
  endif()
endif()
