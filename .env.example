# Environment variables are read from the process; no .env loader is required.
# Default worker path: build/dev/bin/gomoku-worker[.exe]
# GOMOKU_ENGINE_PATH=/absolute/path/to/gomoku-worker
GOMOKU_WORKERS=2
HOST=127.0.0.1
PORT=3001
